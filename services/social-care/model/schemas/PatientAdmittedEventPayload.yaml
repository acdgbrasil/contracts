$schema: "https://json-schema.org/draft/2020-12/schema"
title: PatientAdmittedEventPayload
description: |
  Flat event payload emitted when a patient on the waitlist is admitted to active care.
  Swift Codable serialization produces top-level camelCase fields.
type: object
additionalProperties: false
required:
  - id
  - occurredAt
  - actorId
  - patientId
  - personId
properties:
  id:
    $ref: ./Uuid.yaml
    description: Unique event identifier (UUID).
  occurredAt:
    $ref: ./IsoDateTime.yaml
    description: Timestamp when the event occurred (ISO 8601).
  actorId:
    type: string
    description: Identifier of the actor who admitted the patient.
  patientId:
    $ref: ./Uuid.yaml
  personId:
    $ref: ./Uuid.yaml
