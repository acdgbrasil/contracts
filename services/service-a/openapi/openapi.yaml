openapi: 3.2.0
info:
  title: Service A Sync Contract
  version: 1.0.0
  description: Synchronous APIs for service-a.
servers:
  - url: https://api.example.com/service-a
paths:
  /v1/resources:
    get:
      operationId: listServiceAResources
      summary: List resources
      responses:
        '200':
          description: Resource list.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServiceAResource'
components:
  schemas:
    ServiceAResource:
      $ref: ../model/schemas/ServiceAResource.yaml
